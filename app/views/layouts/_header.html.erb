  <div class="navbar-inner">
    <div class="container">
      <div class="dropdown">
        <%= link_to(image_tag("domiapp.png", size: "80x80", alt: "DOMI", id: "logo"), root_path) %>
        <button class="btn btn-primary btn-large dropdown-toggle pull-right" data-toggle="dropdown">My profile<span class="caret"></span></button>
        <span class="dropdown-arrow"></span>
        <ul class="dropdown-menu">
          <li><%= link_to "Profile", root_path %></li>
        </ul>
        <%= link_to "List your place", new_listing_path, class: "btn btn-info btn-large pull-right" %>
      </div>
    </div>
  </div>
<style type="text/css">
  button {
    margin-top: 10px;
  }
</style>
<!-- Default look -->
 <% if user_signed_in? %>
            
            <%= link_to "Signout", destroy_user_session_path %>
          <% else %>
            <%= link_to "Sign Up!", new_user_registration_path %>
          <% end %>